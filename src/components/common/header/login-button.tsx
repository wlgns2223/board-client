"use client";
import { Button } from "@material-tailwind/react";

export default function LoginButton() {
    return (
        <Button className="bg-blue-500 px-4 py-2 text-base h-9 flex items-center ">
            {"로그인"}
        </Button>
    );
}
